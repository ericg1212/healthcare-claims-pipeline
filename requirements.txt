# ── Core orchestration ───────────────────────────────────────────
dagster==1.12.22
dagster-dbt==0.28.22          # requires dbt-core>=1.7,<1.12

# ── dbt ──────────────────────────────────────────────────────────
dbt-core==1.9.10              # stable LTS; within dagster-dbt range
dbt-snowflake==1.9.4          # matches dbt-core 1.9.x
dbt-duckdb==1.9.6             # CI adapter — no Snowflake credits in CI

# ── FHIR parser ──────────────────────────────────────────────────
pydantic==2.11.3              # FHIR resource models + validation
snowflake-sqlalchemy==1.7.3   # Snowflake bulk load
snowflake-connector-python==3.14.0

# ── Data utilities ───────────────────────────────────────────────
pandas==2.2.3
pyarrow==20.0.0

# ── Testing ──────────────────────────────────────────────────────
pytest==9.0.3
pytest-cov==6.1.0

# ── CI / security ────────────────────────────────────────────────
flake8==7.2.0
bandit==1.8.3
pip-audit==2.9.0
